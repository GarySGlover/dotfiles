;;; kanata --- Summary
;;; Commentary:

;;; Code:
(defcfg
 process-unmapped-keys
 yes
 linux-dev
 /dev/input/event0:/dev/input/by-id/usb-Clover_Clover_Split-event-kbd)

(defsrc)

(deflayermap
 base
 caps
 (tap-hold 200 200 esc lctl)
 spc
 (tap-hold 200 200 spc lalt))

;;; kanata.kbd ends here
